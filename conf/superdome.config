/*
 * -------------------------------------------------
 *  Nextflow config file for use with Docker
 * -------------------------------------------------
 * Configuration for HPE Superdome Flex
 */

params {
  igenomes_base = 's3://ngi-igenomes/igenomes/'
}

process {
   $spades{
     cpus = 40
     memory = 800.GB
  }
}
